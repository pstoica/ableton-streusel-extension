EXTENSION_HOST_PATH=/Applications/Ableton Live 12 Beta.app/Contents/Helpers/ExtensionHost

# AI generation keys are NOT set here — they're entered in-app via
# "Streusel: AI settings…" and stored in the extension's storage directory.
